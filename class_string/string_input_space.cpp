#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;
    cin.ignore();
    string text;
    // cin >> text;
    getline(cin, text);

    cout << n << endl;
    cout << text << endl;

    return 0;
}